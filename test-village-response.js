const { Pool } = require('pg');
const { drizzle } = require('drizzle-orm/node-postgres');

async function run() {
  const fetch = require('node-fetch');
  // I will just read the code of village.service.ts
}
run();
